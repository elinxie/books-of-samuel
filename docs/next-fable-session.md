# Next Fable session (RETIRED — Fable is not used on this project)

**Fable is not used on this project as of 2026-08-24 (user directive) — see
`CLAUDE.md`'s "Model policy — do not invoke Fable."** This file is kept only
because it's linked from elsewhere; it no longer describes a real queued
session. Review-tier work (what this file used to call "Fable spends") now
lives entirely in `docs/fable-review-queue.md`'s Open table and gets decided
directly by whatever session picks it up — see that file's own top note.

Everything below this line is historical (last updated 2026-07-16) and
should not be treated as current guidance; check `docs/next-run.md` and
`docs/fable-review-queue.md` instead.

---

**The 2026-07-16 session's item — the M3 sign-off review — is done.** M3 is
`in-progress` (approved as built; `released` gated only by fable-review-queue
#13/#16/#17 page-verification), ADR-012 (DEM sourcing) and the ADR-009
funerary-burning extension are ratified, and queue #12/#14 are resolved. See
`docs/run-log.md`'s 2026-07-16 entry.
