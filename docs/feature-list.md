# Feature list

The maintained, structured feature list is `src/data/features.ts` (rendered
in-app at `/features`). This file is a pointer, not a duplicate, to avoid drift.

Quick summary as of this commit: landing/passage/location/period entry, first-person
observer (inspect+walk), Ziklag aftermath scene with scripted reenactment, timeline/
replay controls, teleport panel, sources/scholarly-notes/labels toggles
(theological-commentary toggle present but disabled), quality modes, evidence-basis
badges, "certain vs. reconstructed" panel, progress/features/sources/method pages —
all `done` (Milestone 0–1). See `/features` in-app or `src/data/features.ts` for the
full list including `planned` items by milestone.

When adding a feature, edit `src/data/features.ts` — don't hand-edit a list here.
